spring:
  thymeleaf:
    mode: HTML
  profiles:
    active: dev
  messages:
    basename: i18n/messages
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/java-blog?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8888

comment.avatar: /images/avatar.png